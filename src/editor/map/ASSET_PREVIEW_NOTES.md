# Asset preview behavior

The Map Editor asset browser renders registered sprites at their full aspect ratio. Functional Ascension entities that still use Pixi Graphics (NPCs, wolves, sludge and gathering nodes) use a visual fallback matching their in-game silhouette until a sprite is assigned. Imported animated assets are previewed live in the browser and hover preview.
